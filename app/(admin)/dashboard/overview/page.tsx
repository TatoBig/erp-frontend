"use client";
import PageInfo from "@/components/core/PageInfo";

const Dashboard = () => {
  return <PageInfo header={"Visualización general"} key="dashboard">Hello</PageInfo>;
};

export default Dashboard;
